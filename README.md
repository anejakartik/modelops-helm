# modelops-helm — ML lifecycle Helm charts

**One-liner:** Helm charts and templates for reproducible ML training and serving on Kubernetes.

## Stack
Kubernetes, Helm, MLflow, S3, Prometheus

## CI
See `.github/workflows/ci.yml`