# modelops-helm charts

This folder will contain Helm chart templates for training and serving ML models.

Skeleton:
- charts/training
- charts/serving

Add `values.yaml` and templates for each chart.